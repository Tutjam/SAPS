# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

+nocsrf
POST        /messages/check        controllers.SMSController.check

+nocsrf
POST        /subscriptions         controllers.SubscriptionController.update
